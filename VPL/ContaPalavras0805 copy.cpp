#include <ifstream>
#include <iostream>
#include <map>

int main() {
  cin >> file;
  FILE(file);
  int total;
  str::string str1, str2;
  map<std::string, int> palavras;
  while (!= EOF) {
    getline(file, str1);
    tolower(str1) while (str1 != '\n') {
      getline(str1, str2, ' ' || '-');
      palavras.insert(str2, i++);
      if (palavras.find)
        i++ else { palavras.insert }
    }
  }
  cout << palavras.word << ' ' << palavras.counter
}
}
