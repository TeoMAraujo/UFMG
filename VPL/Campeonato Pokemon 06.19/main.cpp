// NÃO ALTERE ESSA LINHA
#include "avaliacao_basica_pokemon.hpp"
#include <iostream>
using namespace std;
int main() {
    char n
    campeonato campeonato;
    while(true){
    std::cin>> n;
    switch (n){
        case: 't'{
            string nome;
            cin << nome;
            campeonato.cadastrar_pokemon(nome)
        }
        case: 'i'{
            string idt;
            campeonato.imprimir_informacoes_treinador(idt)
        } 
        case: 'e'{
            string nome, tipo_ataque;
            double forca_ataque, potencia_raio
            int id;
            cin >> id >> nome >> tipo_ataque >> forca_ataque >> potencia_raio;
            campeonato._treinadores[id]->cadastrar_pokemon_eletrico(std::string nome, std::string tipo_ataque, double forca_ataque, double potencia_raio);
        }
        case: 'q'{
        string nome, tipo_ataque;
            double forca_ataque, potencia_raio
            int id;
            cin >> id >> nome >> tipo_ataque >> forca_ataque >> potencia_raio;
            campeonato._treinadores[id]->cadastrar_pokemon_eletrico(std::string nome, std::string tipo_ataque, double forca_ataque, double potencia_raio);
        }
        case: 'x'{
            string nome, tipo_ataque;
            double forca_ataque, potencia_raio
            int id;
            cin >> id >> nome >> tipo_ataque >> forca_ataque >> potencia_raio;
            campeonato._treinadores[id]->cadastrar_pokemon_eletrico(std::string nome, std::string tipo_ataque, double forca_ataque, double potencia_raio);
        }
        case: 'h'{
            cin << idt1 << idpk1 << idt2 << idpk2;
            campeonato.executar_batalha;
        }
        case: 'b'{
            avaliacao_basica();
        }
    }
    return 0;
}
